extends partials/header

block content 
    main.blogMain 
        .container-fluid
            .row
                .postContent.col-md-12.col-lg-9.d-flex.flex-column.p-4
                    h1= blogPost.heading
                    img.img-fluid(src= blogPost.image_path)
                    p!= blogPost.post_text
                .suggestionsPane.col-md-5.col-lg-3.p-4
                    h2 Also read 
                    if (locals.postTiles)
                        each post in postTiles
                            a(href=`/${post.url}`) 
                                .postTile
                                    img.img-fluid(src= post.image_path, alt="")
                                    h4= post.heading 
    include partials/footer
            
