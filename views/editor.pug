doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
        main 
            form(action="submit", method="post") 
                label(for="title") Post Title 
                input#title(type="text", name="title") placeholder
                label(for="tags") Post Title 
                input#tags(type="text", name="tags") placeholder
                label(for="author") Post Title 
                input#author(type="text", name="author") placeholder
